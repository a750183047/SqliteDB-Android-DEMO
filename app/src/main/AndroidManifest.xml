<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="yan.com.taobaodb" >
    <uses-feature android:name="android.hardware.camera" android:required="true"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>


    <application
        android:allowBackup="true"
        android:icon="@drawable/iconic"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        android:largeHeap="true">
        <activity
            android:name=".activity.LogIn"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activity.SignUp">

        </activity>
        <activity android:name=".activity.MyShop"
                  >

        </activity>
        <activity android:name=".activity.CreateMerchants"
            android:label="创建商店"
            android:theme="@android:style/Theme.Holo.Dialog"
            >

        </activity>

        <activity android:name=".activity.TakePhoto">

        </activity>

        <activity android:name=".activity.Customer">

        </activity>
        <activity android:name=".activity.Admin">

        </activity>
    </application>

</manifest>
